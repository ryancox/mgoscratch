language: go

go:
  - 1.2
  - 1.3
  - tip

before_install:
  - sudo apt-get update
  - sudo apt-get install -q -y supervisor mongodb

install:
  - go get gopkg.in/mgo.v2/bson

before_script:
  - make startdb

script: go test -gocheck.v

after_script:
  - make stopdb
